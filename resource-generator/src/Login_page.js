import React from 'react';
class LoginPage extends React.Component {
    //This is a page the user will see when they open 
    //Resource genrator and they already have an account created. 
    //So if their email is already detected in the database, then they 
    //will be transfered to this page. 
    render(){
        return (
            <div className="LoginPage"></div>)
        }
    }